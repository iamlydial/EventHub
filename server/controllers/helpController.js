//This is the file for Route handler for displaying help and support for user:


const express = require('express');
const session = require('express-session');
const router = express.Router();
const db = require('../db'); 






module.exports = router;